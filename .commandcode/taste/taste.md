- Never write to real `.env` files — only `.env.example` (and similar example/template files) may be edited; keep secrets out of committed files. Confidence: 0.9
- Codifies project rules and environment/setup notes as explicit rules in the repo's agent instructions file (AGENTS.md) so they apply project-wide. Confidence: 0.75
- Wants local/infra setup facts documented (e.g. "Supabase runs in Docker under WSL") so the agent remembers environment context. Confidence: 0.6
- Expects issue-tracker tickets to be updated when their work is done — mark the ticket resolved, tick its checklist, and add a comments note following how already-finished tickets were recorded — and committed, not just the code. Confidence: 0.75
- Wants demo/sample data files (e.g. a seed CSV for manual testing) saved into the repo's `public/` directory as real project files — not just a scratch temp — so they're reachable by URL alongside the running app for manual upload/testing. Confidence: 0.6
- Reports problems by pasting raw console/test output (e.g. a runtime `DeprecationWarning`) with no commentary, expecting the agent to trace it to the root cause and fix it rather than suppress it. Confidence: 0.55
- Reports bugs tersely — sometimes just naming the affected account/auction (e.g. "check whats the problem ... in this account <email>"), sometimes only the symptom (e.g. "i cant start auction ... but i want to start the same one"), and sometimes a hedged observation of an intermittent UI symptom (e.g. "i think the live auction is reloading every time") — and expects the agent to investigate the real/persisted state and reproduce the failure to find the true root cause instead of asking for more detail or guessing. Confidence: 0.7
- Prefers existing app/database data left intact — when offered to bulk-clear real auction data to make behavior match expectations, chose to keep it as-is rather than let the agent modify records; but stray records that block work (e.g. a Live Auction left behind by a test run, blocking the next start) should be deleted once the agent has confirmed no real data is involved — separate real data from test debris. Confidence: 0.6
- Runs the app locally for hands-on testing (their own `next dev` server, e.g. on port 3000) and expects the agent to work alongside that setup — e.g. point browser/Playwright runs at the already-running dev server (PLAYWRIGHT_BASE_URL) rather than starting a second one; will bring the local stack (and WSL/Supabase) back up when asked so verification can finish. Confidence: 0.6
- Prefers comprehensive documentation that covers all pages/features in full detail — a complete inventory and thorough treatment of each, not summaries or partial coverage. Confidence: 0.85
- Wants technical deliverables translated for design audiences — code concepts expressed as design-ready specifications so designers can understand and act on them. Confidence: 0.8
- Values codebase-first understanding — deeply explores the actual implementation (source files, ADRs, architecture, product spec) before producing any documentation; the user is patient with this thorough exploration and will say "continue" rather than requesting shortcuts when it hits limits. Confidence: 0.75
- Wants design system docs (e.g. Design.md) to be a single comprehensive source of truth — covering brand foundations, color tokens for both light and dark modes, typography, spacing rhythm, full component inventory, surface-by-surface composition guide, motion, accessibility, copy rules, and a "smell test" to keep AI-generic UI out — kept at the repo root and updated whenever a new primitive or surface is added. Confidence: 0.85
- Wants the visual language derived from a concrete reference asset in the repo (e.g. `public/background.png`) — extract palette, tone, and motifs directly from the supplied image rather than inventing a generic SaaS aesthetic; the image is treated as the canvas, not decoration. Confidence: 0.75
- Wants shadcn used throughout the website ("use shadcn components all throughout") and the appropriate `shadcn` skill activated when working with it; shadcn primitives are the building blocks for everything, with project-specific composites layered on top. Confidence: 0.8
- Wants design work done through the matching domain skill (e.g. activate `design` skill for design system work) and the agent to pick the right tool automatically without being told which skill to use. Confidence: 0.7
- Expects new UI primitives, composite surfaces, and a public barrel index (`src/components/arena/index.ts`) to ship together as one cohesive system — primitives, composites, and barrel re-exports delivered in a single coordinated change rather than piecemeal. Confidence: 0.7
- Expects verification after non-trivial UI/design work — runs `typecheck`, `lint`, and `format:check` (and applies prettier formatting) before declaring a design-system change done, and re-runs them after format pass. Confidence: 0.7
- Wants tasks carried through to completion without pausing mid-task — finish outstanding work autonomously instead of stopping and waiting for a "continue" prompt. Confidence: 0.7
- Prefers immersive surfaces (e.g. auth) locked to permanent dark mode with no light/dark toggle. Confidence: 0.75
- Dislikes glow/shadow hover effects on buttons — prefers flat minimal hover states with no glow. Confidence: 0.8
- Requires strong text/UI contrast ratios for readability and accessibility. Confidence: 0.75
- Prefers auth cards as dark translucent glass over background imagery, not solid white cards — match the provided mockup. Confidence: 0.75
- Prefers plain divider lines with text (e.g. \"Or\") and no background pill/chip behind the text. Confidence: 0.7
- Prefers buttons to show pointer cursor on hover for clear click affordance. Confidence: 0.7
- Prefers OTP inputs as separate gapped boxes with individual rounded borders, not glued together in a connected strip. Confidence: 0.7
- Rejects explanatory subtext sitting under a title/heading — calls it "AI slopped". The fact a heading would otherwise explain belongs as a compact inline hint (a short monospace fragment) beside the control it constrains, not deleted and not hidden behind an info popover. Confidence: 0.85
- Prefers a restrained, information-dense "instrument panel" register on repeated-use working surfaces (dense monospace numerals, hairline dividers, minimal colour) over dramatic, glowing, animated UI. Confidence: 0.6
- Asks for critical architecture review — wants thorough analysis of their architectural decisions to identify what they did wrong, expecting deep root-cause analysis rather than surface-level summaries. Confidence: 0.6
- Iterates test expectations against real system behavior — when a test's expected outcome conflicts with actual (correct) system behavior, adjusts the assertion to match reality rather than forcing the code to fit an incorrect assumption. Confidence: 0.75
- Tests concurrency/serialization by racing two concurrent calls (via `Promise.all`) against the same expected revision, asserting exactly one "accepted" and one "rejected". Confidence: 0.7
- Runs `npx tsc --noEmit` as a pre-test gate before invoking vitest when working on TypeScript changes. Confidence: 0.7
- Inlines TypeScript type generics in test DB query calls (`pool.query<{...}>`) so assertions on query results are type-checked. Confidence: 0.65
- Inserts an `if (outcome.status !== "accepted") return;` guard after status assertions to narrow union types for subsequent assertions on `outcome.result`. Confidence: 0.65
- Uses descriptive scenario tags as fixture identifiers (e.g. `buildLiveAuction("direct-create")`) to isolate test state and self-document the case. Confidence: 0.6
